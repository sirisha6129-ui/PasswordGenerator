# Password Generator

A simple Python project that generates strong random passwords using letters, numbers, and symbols.

## Features
- User inputs password length
- Generates letters, numbers, and symbols
- Useful for learning Python and security

## Technology Used
- Python

## How to Use
1. Run `main.py`
2. Enter desired password length
3. Copy generated password# PasswordGenerator
A simple Python project that generates strong random passwords using letters, numbers, and symbols.
